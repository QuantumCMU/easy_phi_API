# this is modules required by Easy Phi web application. Install dependencies with:
# sudo pip install -r requirements/production.txt

# Required for production
tornado
pyserial
pyudev
dicttoxml

# Required for development only
mkdocs
